#
# Sample .env file with no configured values. Use this file as a base for
# configuring new environments (e.g. 'staging.env' or 'production.env').
#
# All environment variables consumed by the application during runtime must be
# defined with the "ASPNETCORE_" prefix. Variables without the prefix won't be
# added to the application's IConfiguration.
#

# Base directory where the application's configuration file is located.
ASPNETCORE_CONFIGURATION_BASE_DIRECTORY=

# Path to the application's configuration file, relative to ASPNETCORE_CONFIGURATION_BASE_DIRECTORY.
ASPNETCORE_CONFIGURATION_FILENAME=
